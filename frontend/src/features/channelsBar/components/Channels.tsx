import { ethers } from "ethers";
const Channels = ({ contract }: { contract: ethers.Contract }) => {
  return (
    <div className="relative h-full pt-[50px] pb-[53px]">
      <ul className="h-full"></ul>
    </div>
  );
};

export default Channels;
